# PORT
# Avoid 6000 (browsers block it as an unsafe port -> "Failed to fetch")
PORT=5005

# Db Configurations
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=zera
DB_PORT=5432

# JWT secret used for signing/decoding auth tokens
JWT_SECRET=replace_me
